# 일일 칼로리 기록 기능 추가 상세 정리

- 작성일: 2026-04-25
- 참조 링크: https://chatgpt.com/s/cd_69ec230b35388191a3907c075e890d2f
- 원문 제목: Add daily calorie record feature
- 문서 성격: 기능 요구사항/설계 초안 (ongoing)

## 1) 기능 목표
사용자가 **하루 단위로 섭취 칼로리와 소모 칼로리**를 기록하고, 순칼로리(섭취-소모) 및 목표 대비 달성률을 확인할 수 있도록 한다.  
핵심 목표는 다음과 같다.

1. 사용자가 식단/운동 기록을 빠르게 입력할 수 있어야 한다.
2. 하루 합계/순칼로리를 즉시 계산해서 보여줘야 한다.
3. 최근 7일/30일 추세를 통해 습관 개선 인사이트를 제공해야 한다.
4. 기존 퀘스트/루틴 기능과 연결 가능한 확장성을 가져야 한다.

---

## 2) 사용자 시나리오

### 시나리오 A: 식사 후 섭취 칼로리 기록
1. 사용자가 "칼로리 기록" 화면 진입
2. "섭취 추가" 버튼 탭
3. 음식명(선택), 칼로리(kcal), 메모(선택), 시간 입력
4. 저장 즉시 해당 날짜 섭취 총합 업데이트

### 시나리오 B: 운동 후 소모 칼로리 기록
1. 사용자가 "소모 추가" 버튼 탭
2. 운동명(선택), 소모 칼로리(kcal), 시간, 메모 입력
3. 저장 즉시 해당 날짜 소모 총합 및 순칼로리 갱신

### 시나리오 C: 일간/주간 추세 확인
1. 사용자가 오늘 카드에서 "최근 7일" 탭 전환
2. 날짜별 섭취/소모/순칼로리 막대 또는 라인 차트 확인
3. 목표 초과/미달 상태를 색상 배지로 확인

---

## 3) 기능 요구사항 (Functional Requirements)

### FR-01. 일일 칼로리 요약
- 날짜별로 다음 값을 계산/표시한다.
  - 섭취 총합(`intakeTotalKcal`)
  - 소모 총합(`burnTotalKcal`)
  - 순칼로리(`netKcal = intakeTotalKcal - burnTotalKcal`)
  - 목표 대비 달성률(`progressPercent`)

### FR-02. 기록 CRUD
- 섭취/소모 항목 각각 생성(Create), 수정(Update), 삭제(Delete) 가능
- 항목 필수값: 타입(섭취/소모), 칼로리, 기록 시각
- 옵션값: 제목(음식/운동명), 메모, 태그

### FR-03. 날짜 단위 조회
- 기본 조회 범위: 오늘
- 추가 조회: 특정일, 최근 7일, 최근 30일
- 타임존 기준은 사용자 로컬 타임존으로 계산

### FR-04. 목표 설정
- 사용자별 일일 목표 칼로리 설정 가능
- 목표 미설정 시 기본값(예: 2000kcal) 또는 "미설정" 상태로 처리

### FR-05. 상태 피드백
- 목표 초과/적정/미달에 따라 색상 상태 표시
- 저장/수정/삭제 시 토스트 피드백 제공

---

## 4) 비기능 요구사항 (Non-Functional Requirements)
- 입력 반응성: 저장 액션 후 1초 이내 UI 반영
- 데이터 정합성: 중복 저장/음수 값 저장 방지
- 확장성: 향후 음식 DB 연동 및 자동 칼로리 추정 기능 추가 가능 구조
- 접근성: 숫자 입력 필드 라벨, 버튼 접근성 텍스트 제공

---

## 5) 데이터 모델 초안

### 컬렉션 구조 예시 (Firestore)
`users/{uid}/calorieLogs/{logId}`

```json
{
  "type": "intake",          // "intake" | "burn"
  "kcal": 450,
  "title": "점심 - 닭가슴살 샐러드",
  "memo": "드레싱 반만 사용",
  "loggedAt": "2026-04-25T12:30:00+09:00",
  "localDate": "2026-04-25", // 집계 키
  "createdAt": "serverTimestamp",
  "updatedAt": "serverTimestamp"
}
```

### 사용자 설정 예시
`users/{uid}/settings/nutrition`

```json
{
  "dailyCalorieGoal": 1800,
  "updatedAt": "serverTimestamp"
}
```

---

## 6) 화면/UX 초안

### A. 오늘 요약 카드
- 섭취: `1,650 kcal`
- 소모: `420 kcal`
- 순칼로리: `1,230 kcal`
- 목표 대비: `68%`

### B. 기록 리스트
- 시간순 정렬(최신 우선)
- 항목 좌측 배지: 섭취(+) / 소모(-)
- 항목 스와이프 또는 메뉴: 수정/삭제

### C. 입력 모달
- 필드: 타입, kcal, 제목, 시간, 메모
- 유효성 오류 메시지 즉시 표기

---

## 7) 유효성/에러 처리 규칙
1. kcal은 1~5000 범위 정수만 허용
2. 미래 시각 기록은 허용하되 기본값은 현재 시각
3. 수정/삭제 시 대상 문서 없으면 재조회 후 경고 토스트 표시
4. 네트워크 오류 시 오프라인 큐 또는 재시도 버튼 제공

---

## 8) 구현 단계 제안

### Phase 1 (MVP)
- 기록 생성/조회/삭제
- 오늘 요약(섭취/소모/순칼로리)
- 목표 설정 1개 값

### Phase 2
- 수정 기능, 7일/30일 집계
- 차트 시각화
- 목표 달성률 배지

### Phase 3
- 음식/운동 템플릿
- 반복 입력(최근 항목 재사용)
- 퀘스트 시스템 연동(예: "3일 연속 기록")

---

## 9) QA 체크리스트
- [ ] 섭취/소모 각각 저장 시 합계 즉시 반영되는가
- [ ] 다른 날짜 선택 시 해당 날짜 데이터만 정확히 조회되는가
- [ ] 7일/30일 집계 합산이 일간 데이터와 일치하는가
- [ ] 목표 변경 시 달성률 재계산이 즉시 반영되는가
- [ ] 삭제 후 순칼로리/차트 값이 정확히 갱신되는가

---

## 10) 오픈 이슈
1. 목표 미설정 사용자 기본값 정책 확정 필요
2. 운동 소모 칼로리 자동 추천(운동 종류/시간 기반) 도입 여부
3. 식단 사진 첨부 기능 우선순위
4. 건강 데이터 연동(Apple Health/Google Fit) 범위 검토

---

## 11) 후속 작업
- 본 문서를 기준으로 `SPECS.md`에 확정 요구사항 반영
- 프런트 입력 컴포넌트/집계 훅 설계 문서 분리
- Firestore 보안 규칙에 `calorieLogs` 접근 제어 추가 검토


---

## 12) 칼로리 기록 추가 구현 방식 (IIFE 모듈)

앱의 기존 구조가 IIFE(Immediately Invoked Function Expression) 모듈 패턴을 사용하므로,
칼로리 기록 기능도 동일한 방식으로 분리/추가한다.

### 모듈 분해 제안
1. `calorie-log-repository.iife.js`
   - Firestore CRUD 담당
   - `createLog`, `updateLog`, `deleteLog`, `listLogsByDateRange`
2. `calorie-log-service.iife.js`
   - 도메인 계산 로직 담당(일일 합계, 순칼로리, 목표 달성률)
   - `calculateDailySummary`, `groupByLocalDate`
3. `calorie-log-ui.iife.js`
   - 화면 렌더링/이벤트 바인딩 담당
   - 카드/리스트/입력 모달 갱신
4. `calorie-ai-estimator.iife.js`
   - AI 자동계산 요청/응답 정규화 담당
   - `estimateMealCalories`, `estimateWorkoutCalories`

### 공개 API 예시
```js
window.CalorieLogModule = (function () {
  function init() {}
  function openInputModal(type) {}
  function saveLog(payload) {}
  function refreshDaily(dateKey) {}

  return { init, openInputModal, saveLog, refreshDaily };
})();
```

### IIFE 도입 시 원칙
- 전역 오염 최소화: `window`에는 최상위 진입점만 노출
- 의존성 주입: Firebase/Auth/Toast 유틸은 생성 시점 주입
- 점진 적용: 기존 화면에 토글 방식으로 기능 온보딩
- 장애 격리: AI 추정 실패 시 수동 입력 플로우로 즉시 폴백

---

## 13) AI 기반 칼로리 자동계산 구현

### 목표
사용자의 입력 부담을 줄이기 위해 음식/운동 정보를 기반으로 칼로리를 자동 추정한다.
자동 추정은 "보조 기능"으로 제공하고, 최종 저장 전 사용자 확인/수정을 필수로 한다.

### 입력 채널
1. 텍스트 입력: "닭가슴살 샐러드 1인분", "런닝 35분"
2. 구조화 입력: 음식명 + 중량(g), 운동명 + 시간(분)
3. 이미지 입력(Phase 3): 식단 사진 업로드 후 비전 모델 추정

### 추정 파이프라인
1. 사용자 입력 정규화(단위/오탈자/동의어 처리)
2. 카테고리 분류(식단 vs 운동)
3. AI 추정 호출
4. 신뢰도(score) 및 근거(reason) 수신
5. 앱 내 보정 규칙 적용(최소/최대 범위 클램프)
6. 사용자 확인 후 저장

### 응답 스키마 예시
```json
{
  "type": "intake",
  "estimatedKcal": 520,
  "confidence": 0.82,
  "reason": "닭가슴살/채소/드레싱 평균 열량 기준",
  "suggestedPortion": "1 serving"
}
```

### UX 규칙
- 신뢰도 0.75 이상: 기본값 자동 채움 + "AI 추정" 배지
- 신뢰도 0.50~0.74: 추천값 제시 후 확인 유도
- 신뢰도 0.50 미만: 자동 입력 미적용, 수동 입력 유도
- 항상 "직접 수정" 가능해야 하며, 최종 저장 권한은 사용자에게 있음

### 안정성/비용 통제
- 호출 제한: 사용자별 일일 AI 추정 횟수 제한(예: 30회)
- 캐시: 동일 문구/조건 재입력 시 최근 추정값 재사용
- 타임아웃: 2~3초 내 미응답 시 폴백
- 로깅: 추정 요청/응답/수정 여부를 익명 이벤트로 기록

### 보안/프라이버시
- 민감정보(개인식별정보) 프롬프트 전송 금지
- 이미지 업로드 시 메타데이터 제거 및 저장기간 정책 적용
- 모델 호출 키는 클라이언트가 아닌 서버 함수에서만 보관

### 구현 단계 (AI)
- Phase A: 텍스트 기반 섭취/소모 추정 MVP
- Phase B: 개인화 보정(사용자 과거 수정 패턴 반영)
- Phase C: 이미지 기반 식단 추정 + 멀티모달 확장

---

## 14) AI + 칼로리 기록 연동 QA 체크리스트
- [ ] AI 추정값 적용 후 사용자 수정 시 최종 저장값이 정확한가
- [ ] 신뢰도 구간별 UI 정책(자동채움/권장/수동유도)이 올바른가
- [ ] AI API 타임아웃/실패 시 수동 입력으로 정상 폴백되는가
- [ ] 동일 입력 반복 시 캐시 재사용으로 응답시간이 개선되는가
- [ ] 일일 AI 호출 제한 초과 시 안내 메시지와 차단 동작이 정상인가
