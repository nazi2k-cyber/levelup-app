{
  "name": "levelup-reboot",
  "version": "1.0.624",
  "description": "LEVEL UP: REBOOT - 게임처럼 현실을 레벨업하는 앱",
  "license": "UNLICENSED",
  "main": "www/app.html",
  "scripts": {
    "sync": "cap sync android",
    "add-android": "cap add android",
    "setup:android": "bash scripts/setup-android.sh",
    "build-apk": "cd android && chmod +x ./gradlew && ./gradlew assembleDebug",
    "build-bundle": "cd android && chmod +x ./gradlew && ./gradlew bundleRelease",
    "generate-config": "bash scripts/generate-firebase-config.sh",
    "version-bump": "bash scripts/version-bump.sh",
    "version-sync": "bash scripts/version-bump.sh",
    "backup": "bash scripts/backup.sh",
    "backup:zip": "bash scripts/backup.sh --zip",
    "backup:full": "bash scripts/backup.sh --full",
    "backup:git": "bash scripts/github-backup.sh",
    "backup:bundle": "bash scripts/github-backup.sh --bundle",
    "backup:cros": "bash scripts/cros-package.sh",
    "lhci": "lhci autorun",
    "serve:www": "serve www -p 3000 -s",
    "test:rules": "firebase emulators:exec --only firestore 'jest test/firestore-rules.test.js --testEnvironment node --forceExit'",
    "test:security": "npm run test:rules && npm audit"
  },
  "dependencies": {
    "@capacitor-community/admob": "^6.2.0",
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/geolocation": "^6.1.1",
    "@capacitor/local-notifications": "^6.0.1",
    "@capacitor/push-notifications": "^6.0.2",
    "@codetrix-studio/capacitor-google-auth": "3.4.0-rc.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@firebase/rules-unit-testing": "^3.0.4",
    "@lhci/cli": "^0.14.0",
    "firebase": "^10.8.1",
    "jest": "^29.7.0",
    "serve": "^14.2.0",
    "xlsx": "^0.18.5"
  },
  "overrides": {
    "undici": "6.25.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 30000
  }
}
